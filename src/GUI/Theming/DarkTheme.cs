namespace Sokol.GUI;

/// <summary>A dark Catppuccin-Mocha–inspired theme (the framework default).</summary>
public class DarkTheme : Theme
{
    // DarkTheme uses the base Theme values which are already dark.
    // Override here if you want to deviate from the default dark palette.
}
